/**
 * AI conversation preview shell — interaction design deferred.
 */
export function ConversationPreview() {
  return (
    <div
      data-component="conversation-preview"
      className="min-h-48 border border-white/10 bg-surface p-6 text-sm text-foreground-muted"
    >
      {/* TODO: guided conversation UI */}
      Conversation preview placeholder
    </div>
  );
}
